{% extends ':app:base.html.twig' %}

{% block page_content %}
    <h1>Note creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('notes_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
